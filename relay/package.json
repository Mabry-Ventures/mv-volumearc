{
  "name": "volumearc-ai-relay",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that proxies iOS coach requests to the Gemini API with SSE streaming, App Attest auth, and per-device rate limiting.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260410.0",
    "typescript": "^5.6.3",
    "vitest": "^4.1.11",
    "wrangler": "^4.94.0"
  },
  "dependencies": {
    "@peculiar/x509": "^2.0.0",
    "cbor-x": "^1.6.4",
    "reflect-metadata": "^0.2.2"
  }
}
